APP_ENV=development
APP_NAME=URIM
ENGINE_NAME=Kairos
APP_VERSION=0.1.0
DEFAULT_LOCALE=fr-CD
DEFAULT_CURRENCY=CDF

DATABASE_URL=
REDIS_URL=
OBJECT_STORAGE_URL=

ENABLE_LIVE=false
ENABLE_REAL_BETTING=false
ALLOW_TEST_FIXTURES=false
ALLOW_PRODUCTION_MOCKS=false

# Phase 1 intentionally defines no real sports-provider API keys.
# Add provider secrets later through a secret manager, never in Git.
